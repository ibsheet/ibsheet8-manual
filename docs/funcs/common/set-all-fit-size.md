# setAllFitSize ***(method)***

<!-- synonyms: set all fit size, auto fit all columns, fit all columns, adjust all column width, all fit width, autosize all, 전체 컬럼 자동 맞춤, 전체 열 너비 조정, 컬럼 폭 자동, 열 자동 맞춤, 열 너비 일괄 조정, setAllFitSize 메소드 -->

> fitSize 를 이용하여 전체 컬럼의 너비를 조정합니다. 

### Syntax
```javascript
void setAllFitSize();
```

### Return Value
***none***

### Example
```javascript
// fitSize를 이용하여 전체 컬럼의 너비를 조정합니다.
sheet.setAllFitSize();
```

### Read More

### Since

|product|version|desc|
|---|---|---|
|core|8.0.0.0|기능 추가|
